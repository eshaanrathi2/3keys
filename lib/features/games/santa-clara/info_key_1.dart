import 'package:flutter/material.dart';

class InfoKey1 extends StatelessWidget {
  const InfoKey1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Info of Key 1"),
      ),
    );
  }
}